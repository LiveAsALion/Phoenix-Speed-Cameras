# getspeedshield.com site source

Deploy artifacts for the SpeedShield marketing/QR-redirect site
(landing page + one-shot VPS bootstrap). No camera data here; the
camera dataset remains `camera_data.json` at the repo root.
