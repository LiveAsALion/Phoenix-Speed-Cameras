name: Update Phoenix Speed Cameras

on:
  schedule:
    # 11:30 UTC = 4:30 AM Phoenix MST
    - cron: "30 11 * * *"
  workflow_dispatch: {}

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name
